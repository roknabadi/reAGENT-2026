# Secrets and local credentials
.env
.env.*
!.env.example
.secrets/
*.pem
*.key
credentials*.json
.mcp.local.json
.claude/settings.local.json

# Local environments and generated state
.venv/
.proto/
.paperclip/
.modal.toml
.cache/
tools/
bin/
downloads/
apps/
vendor/
outputs/
benchflow/jobs/
benchflow/artifacts/
benchflow/matrix.yaml

# Kevin's discovery pipeline regenerates these locally (stage0 -> stage1).
# 400MB+ of DepMap bulk files and a 28MB dependency table; per his handoff and
# DECISIONS.md, generated artifacts stay out of Git.
tests/re-agent_discovery/data/

# proto-tools deploy logs
logs/

# Agent run state and large structural artifacts stay out of Git.
# Runs are reproducible from the input bundle plus the recorded config hash.
runs/
__pycache__/
*.pyc
*.egg-info/
.DS_Store

# Local shell wiring and MCP scratch output, not project state.
.envrc
.playwright-mcp/

# Boltz smoke output: a generated artifact, like everything under runs/.
boltz-experiments/
