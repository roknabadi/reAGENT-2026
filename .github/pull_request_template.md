## What changed

## Why

## Public sources / data used
<!-- Recorded in SOURCES.md? Public only — see SOURCE_POLICY.md. -->

## How it was tested
<!-- Paste what actually ran. "Not tested" is a valid answer; guessing is not. -->

```
python -m unittest discover -s tests -v
```

## What is still uncertain
<!-- Say what you did NOT verify. This is the useful half of the PR. -->

---

- [ ] Merged current `main` and reran tests
- [ ] Staged explicit paths — no `.env`, no downloaded data, no generated output
- [ ] Observed evidence, computed results, predictions, and hypotheses are
      labeled separately; no claim of binding, safety, efficacy, or
      experimental validation
- [ ] If this passes a gate in `team/CHECKPOINTS.md`, the named human signed it
